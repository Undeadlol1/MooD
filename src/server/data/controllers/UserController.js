// import UserModel from "../models/user.js"

export default class User {
    findOne (selector) {
        // find user  
        // UserModel.findOne({})
    }

    create (selector) {
        // create user
        // UserModel.create()
    }
}
